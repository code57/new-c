#include <stdio.h>

int main()
{
	int arr[5] = { 0 };
	int i = 0;
	int max = 0;
	
	while (i < 5)
	{
		scanf("%d", &arr[i]);
		max = max < arr[i] ? arr[i] : max;//利用三目运算符求最大值
		i++;
	}
	printf("最大值是%d\n", max);

	return 0;
}
